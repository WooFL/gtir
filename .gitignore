node_modules/
.gtir/
*.log
# generated by scripts/bundle-grammars.mjs (prepack); shipped in the npm tarball via package.json "files"
# anchored to repo root so it does NOT also match vendor/grammars/ (whose README is tracked)
/grammars/
# shader grammar wasm — built from source, not committed (~5MB); regen: npm run build:shaders
vendor/grammars/*.wasm
# local planning/spec scratch — not part of the shipped project
docs/superpowers/
